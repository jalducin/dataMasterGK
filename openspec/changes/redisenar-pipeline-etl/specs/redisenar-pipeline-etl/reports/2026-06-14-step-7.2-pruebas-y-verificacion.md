# Reporte Step 7.2 — Pruebas y verificación de estado

- Fecha: 2026-06-14
- Cambio: redisenar-pipeline-etl
- Agente: Claude (Opus 4.8)
- Rama: feature/redisenar-pipeline-etl

## Comandos ejecutados

- `python -m pytest tests/ -v`
- `python -m py_compile app.py interface_runner.py scheduler.py init_db.py src/*.py src/classes/*.py`
- `python app.py` (arranque real) + `curl` a los endpoints
- `python c:\tmp\capture_golden.py` (captura del XML golden pre-refactor)

## Resultados de pruebas

- Dirigidas + suite completa: **10 pasaron, 0 fallaron, 0 omitidas**.
  - `test_xml_golden.py` (4): el XML de Tiendas, Operadores, Promociones y Categorías
    es **byte-idéntico** al golden pre-refactor (timestamps normalizados). Garantiza que GK
    recibe exactamente el mismo formato.
  - `test_pipeline.py` (2): el pipeline genera **un único** registro en `XML_Generados`
    (antes 2-3 por la doble auditoría), inserta la fila maestra de `Operadores`
    (antes fallaba por la columna inexistente `login`), envía **una sola vez**
    (antes 2x por la doble ejecución) y mueve el Excel a procesados (idempotencia).
  - `test_endpoints_seguridad.py` (4): `/filtrar_fecha` y `/descargar_csv` devuelven **400**
    ante tipos fuera de la lista blanca e intentos de inyección; aceptan los tipos válidos.
- Compilación: OK (sin SyntaxError en ningún módulo).
- Duración: ~1.6 s.

## Verificación manual (Step 7.3) — HTTP + ETL

Arranque real de `python app.py` (scheduler en segundo plano, sin errores en el log):

| Endpoint | Resultado |
|----------|-----------|
| `GET /` (panel) | HTTP 200 |
| `GET /static/css/tailwind-vendor.css` (CSS local, ya no Play CDN) | HTTP 200 |
| `GET /filtrar_fecha?tipo=XML_Generados` | HTTP 200 |
| `GET /filtrar_fecha?tipo=Operadores` (no permitido) | HTTP 400 |
| `GET /descargar_csv?tipo=sqlite_master` (no permitido) | HTTP 400 |
| `GET /logs_recientes` | HTTP 200 |

El pipeline ETL se verificó vía las pruebas end-to-end con directorios temporales y un
transmisor falso (sin necesidad de servidor SFTP real): generación de XML, envío con una
sola conexión, auditoría única y archivado idempotente.

## Verificación de estado

- Antes (código original): Tiendas se procesaba hasta 6× (2× por `interface_runner` × 3×
  por bucles duplicados en `store.py`); conexión SFTP/SQLite por elemento; `Operadores`
  nunca registraba filas; XML con 2-3 registros de auditoría.
- Después: 1× procesamiento por archivo; 1 conexión SFTP por corrida; 1 conexión SQLite por
  corrida con commit único; `Operadores` registra fila; 1 registro de auditoría por XML.
- Estado restaurado: Sí — las pruebas usan `tmp_path`; la BD/archivos reales no se modificaron.
  El servidor de prueba se detuvo tras la verificación.

## Resultado

- Estado Step 7.2: **PASS**
- Bloqueos: ninguno.
- Nota: el CSS vendorizado cubre las clases usadas hoy por las plantillas; si se agregan
  clases nuevas, ampliarlo o generarlo con Tailwind CLI en CI.
